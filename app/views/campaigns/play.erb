<% @title = @campaign.title %>
<div class="container">
  <div class="row">
    <div class="col-sm-10 col-lg-8 col-sm-offset-1 col-lg-offset-2">
      <div class="well">

        <input id="first-beat" type="hidden" value="<%= @my_beat %>">

          <div id="beats-<%= @my_beat %>" class="md-preview">
            <%= Markdown.new(@campaign.beats.find_by(order: @my_beat).content).to_html.html_safe %>
          </div>

          <% @campaign.beats.each do |x| %>
            <div id="beats-<%= x.order %>" class="md-preview" style="display: none">
              <%= Markdown.new(x.content).to_html.html_safe %>
            </div>
          <% end %>
      </div>
    </div>
  </div>
</div>

<nav class="game-nav navbar navbar-default navbar-fixed-bottom">
  <div class="">
    <div class="col-xs-4">
      <button class="btn btn-default prev-beat">PREV</button>
    </div>
    <div class="col-xs-4">
      <button class="btn btn-default">EXIT</button>
    </div>
    <div class="col-xs-4">
      <button class="btn btn-default next-beat">NEXT</button>
    </div>
  </div>
</nav>


<!-- @campaign.beats.each do |x=@my_beat|
      Markdown.new(x).to_html.html_safe
    end
 -->