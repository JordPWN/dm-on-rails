<div class="container">
  <div class="row">
    <div class="col-sm-10 col-lg-8 col-sm-offset-1 col-lg-offset-2">
      <div class="well">
        <h1><%=  @campaign.title %></h1>
        <div class="md-preview">
        <% @campaign.beats.each do |campaign_beats| %>
          <%= Markdown.new(campaign_beats.content).to_html.html_safe %>
        <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<nav class="game-nav navbar navbar-default navbar-fixed-bottom">
  <div class="">
    <div class="col-xs-4">
      <button class="btn btn-default">PREV</button>
    </div>
    <div class="col-xs-4">
      <button class="btn btn-default">EXIT</button>
    </div>
    <div class="col-xs-4">
      <button class="btn btn-default">NEXT</button>
    </div>
  </div>
</nav>